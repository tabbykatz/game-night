import * as React from "react";

import { toast } from "react-hot-toast";

import useApi from "../auth/useApi";

const CreateEvent = () => {
  return <h1>Let's create an event</h1>;
};

export default CreateEvent;
